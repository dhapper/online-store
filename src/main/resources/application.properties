spring.application.name=store
server.port=8081

# Database Configuration: Supabase PostgreSQL (Transaction Pooler)
# Using the Supabase Transaction Pooler (Recommended for most web applications)
# IMPORTANT: Replace the password with your actual Supabase password.
# For production, use environment variables or a secrets management system for the password!
spring.datasource.url=jdbc:postgresql://aws-0-ca-central-1.pooler.supabase.com:6543/postgres
spring.datasource.username=postgres.odwxykdgkwxissbgmxhu
spring.datasource.password=dhapper-supabase-password

# spring.datasource.driver-class-name=org.postgresql.Driver # Usually not needed, Spring Boot auto-detects

# Recommended pooler settings for HikariCP (Spring Boot's default connection pool)
spring.datasource.hikari.maximum-pool-size=10
# Note: idle-timeout has no effect when maximum-pool-size is used without minimum-idle set (default is fixed size pool)
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1800000

# JPA settings
# Use 'update' for initial testing and schema creation/updates based on entities.
# CONSIDER CHANGING THIS TO 'none' FOR PRODUCTION TO PREVENT UNINTENDED SCHEMA CHANGES.
spring.jpa.hibernate.ddl-auto=update
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect # Usually not needed, Spring Boot auto-detects
# Set to false for production
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true